<h1>Breakable Toy Landing Page</h1>

<form action="/add" method="POST">
    <button>Add 'faker' address</button>
</form>

<% addressBook.forEach((value,key,map) => { 
    var fullName = value.firstName + " " + value.lastName %>
    <li><%= fullName %> - <%= value.phoneNumber %></li>
<% }); %>